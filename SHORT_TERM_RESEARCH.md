# 次日条件买入、持有1～2交易日：执行研究模块

版本：short-term-contract-0.1.0；2026-09-11。

这是现有项目的增量实现，不是已训练选股模型。原预测数据库不被打开、覆盖或重建；不连接交易账户，不自动下单。

## 用户目标与必须区分的结果

T为已收盘基础日，E为下一个经交易所核实的交易日。

| 环节 | 口径 |
|---|---|
| T收盘后 | 使用T最终原始行情，冻结全5～10元池、风险与特征 |
| E日09:00研究 | 补充截至09:00已公开消息，不重下全历史，不倒填发布时间 |
| E日条件入场 | 触发并实际成交才产生交易；先触板、过度高开、不满足价位则放弃 |
| E+1 | 买入后下一实际交易日，最早开始卖出；不是自然日加1 |
| E+2 | 买入后第二个交易日，14:50触发时间退出；停牌或无对手盘可能无法卖出 |

原P1/P2/P3a/P3b继续保留，含义不改变。新增执行标签独立统计：

- `P_net_profit`：条件实际成交后，按固定退出规则实现的扣费后净盈利概率。
- `P_limit_after_entry`：实际买入时间之后、持仓退出之前出现触板的概率。
- `P_close_limit_after_entry`：同一持有窗口收盘封板概率。
- 买入当天涨停后第二天亏损卖出：触板可以成功，交易净盈利必须记失败。
- 股票当天上涨不等于交易盈利；触发条件不等于委托成功，更不等于成交。

80%仅是待检验目标，不是承诺、不写成最低输出值、不由评分换算。若只统计近期少数已卖出的盈利单、忽略被跌停锁住的仓位，胜率会被高估，本实现单列所有未成交、未退出、费用缺失及协议违规记录。

## 已实现的文件

- `close_snapshot.py`：沿用真实完整分页的供应商主表，按50只一组请求带当日时间戳的盘后OHLCV和相关字段；逐批落盘，保留错误、遗漏代码、原始响应、SHA256。供应商状态和限价不冒充官方认证。无涨跌停/停牌/全池历史验证时仍为DATA NOT READY。
- `short_term.py`：原始日线特征、限价证据缺失处理、条件单准入、版本化价格公式、成交与T+1核对、买入后涨停时序、扣费后净收益及分母核对。
- `test_short_term.py`：隔离的合成工程测试；从不写入真实行情或预测样本。云端另用真实历史产物执行`audit-source`，两类验证不混用。

## 价格计算候选规则（待真实时间外验证）

这套公式是固定的研究假设，尚未通过收益回测，不能宣传有80%胜率。

初始候选必须来自完整冻结池及市场→板块→个股筛选；同时有真实催化并已反向排雷。不允许将数据接口验收的3只股票当作候选。

为了研究“买入后才启动”，初版不接纳T日已触板/连板或状态不明的股票，并暂时排除5日已涨超15%或20日已涨超30%的过度延伸结构。此为事先固定的实验筛选，不是回测得出的最佳阈值，也不保证剩余股票有优势。

报价tick必须取该证券实际规则，不能对所有品种一律假设。以下为待验证的突破型限价方案：

1. 触发价 = max(T收盘价, T之前20日最高价) + 一个报价单位，向上按tick取整。
2. 买入委托上限 = min(触发价+0.15×ATR14, T收盘×1.025, E日官方涨停价−tick)，向下取整。上限低于触发价则不出单。
3. 失效/止损触发 = min(最近20日最低价−tick, 触发价−0.8×ATR14)，向下取整。以买入上限估计的风险距离大于5%则剔除。
4. 止盈观察触发 = 买入上限 + 2×(买入上限−止损触发)，向上取整。这不是保证能成交的目标价，也不随事后走势修改原计划。
5. 只在E日09:35～10:30监控。开盘超过上限、买入前已经触板、盘中先跌破失效位、新增停牌/ST/重大负面、板块证伪，均取消。到10:30撤销未成交部分，不无条件追价。
6. E+1才能执行卖出；E+2的14:50发出时间退出信号。跌停、停牌、系统故障或缺少对手盘时实际退出推迟，保留尾部损失，不能按理想止损价抹平。

程序输出的是券商无关的研究条件，不是已提交的条件单。账户权限、交易单位、资金、券商是否支持多个联合条件/OCO及服务器监控必须另行核实。不能把两个独立买入条件单同时激活造成重复买入；一次持仓只能对应一个冻结计划。未知券商/费用不替用户推定。

## 正式准入与80%验证

DATA READY仅为必要条件。具体价格方案必须另有全池、因子、状态、官方限价、交易日、除权处理、事件排雷及模型证据文件，并核对SHA256。执行模型必须使用本策略版本、扣费/滑点/未成交约束，以及已结束的时间外交易样本，不能拿下一天收盘上涨模型来替代。

至少100个可复算成交样本、40个独立入场交易日只是展示治理下限，不保证统计可靠。训练标签必须在校准集开始前结束；校准标签须在测试前结束；测试结果必须在预测发布前已揭晓。同日股票成簇，持仓跨日与序列相关另外检查。基准须为同日、同制度和同状态的完整池。

不以短期80%命中作为充分条件：还要看校准概率、入场日期簇区间、平均净收益、盈亏比、尾部损失、未平仓、最大回撤、费用与滑点敏感性、无成交率和样本外稳定性。初版价格发布门要求独立验证提供的净盈利概率及日期簇95%下限都不低于80%，期望净收益>0；未通过就空候选，不能为达标改阈值或删亏损样本。

`summarize_trades`只产出历史成交统计，不会把它自动升级成校准预测。它用入场日期delete-one jackknife与描述性Wilson包络防止全赢样本出现虚假[1,1]区间，但仍未解决跨日序列依赖，不是独立模型验收器；正式概率继续要求独立的时间外校准和更完整的时间簇/区块验证。

## 时序与成交证据

仅日线High触及涨停，无法判断是在你买入前还是买入后；同一分钟既触发入场又触板也有路径歧义。没有逐笔/足够精度分时或可信成交回执时相关标签留null。不能用“当日曾涨停”充当“买入后涨停”。

实际成交回执必须含证券、买卖方向、唯一成交ID、时间、价格、数量、全部费用与来源。已成交价本身包含执行滑点影响，不再机械重复扣一次模拟滑点；回测使用明确的费用/滑点模型并记录版本。买入后未卖出时不填实现收益。停牌/跌停无法卖出的仓位不得从统计中删除。

## 当前边界

2026-09-11前序入口验收取得5562只供应商名单、三个市场各570根真实日线；尚不是全市场250日回填。此次新增全市场盘后快照与超短线执行模块须以新的Actions产物为准，不拿本文件代替运行结果。

仍需全池历史、逐日官方状态/限价、时点复权、板块/事件、分时执行样本及训练/校准/测试。没有这些，本模块不会提供个股买入价格或80%概率。本次不改动或创建正式预测记录。

## 原始参考

- 上交所《交易规则（2026年修订）》：https://www.sse.com.cn/lawandrules/sselawsrules2025/trade/universal/c/c_20260424_10816492.shtml （生效2026-07-06；普通A股不属于当日回转交易例外）。
- 华泰证券条件单说明及风险提示：https://download.htsc.com.cn/download/HTSC_LCT_MANUAL_20231130.pdf （触发、委托、成交不同；跳空、流动性、系统状态可能导致无法成交）。
- 沪市2026年休市通知：https://www.sse.com.cn/disclosure/announcement/general/c/c_20251222_10802507.shtml
- 深市2026年休市通知：https://investor.szse.cn/disclosure/notice/general/t20251222_618087.html
- 北交所2026年休市通知：https://www.bse.cn/important_news/200027428.html

规则与公式版本不等于模型版本，接口通过不等于生产准入，不开展自动交易。
