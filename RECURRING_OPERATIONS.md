# 18:00准备与09:00研究

## 2026-09-23 导入与冻结恢复入口

采集成功、运输成功和原行情库成功交接是三个独立结果。当前盘后必须先取得原行情库的最新完整版本；下载报错或字节数不符时保留失败事实，不使用旧库或截断副本，不新建同名库。`sealed_database.py`现在在打开SQLite前检查头部页数与实际字节数。

恢复 `data/evening` 的 `catalog.json`、每个未导入包的 `index.json` 和列出的 `.b64` 分块到本地数据目录。它们是公开行情证据，使用已授权GitHub读取工具即可。随后执行：

```sh
python recover_evening_pipeline.py --db <最新完整原库> --expected-db-sha256 <当前保存哈希> --data-root <已恢复运输目录> --output <全新输出目录>
```

此命令在隔离副本中无损去重逐字段来源、按原日期校验原始响应、跳过已入库SHA、导入未完成增量、封存SQLite并计算最后交易日描述性因子。每包完成后保存 `import_progress.json`。`preparation.json`、`frozen_inputs.zip`与工作副本仍为待持久保存产物，不能被误认为已完成原库交接或DATA READY。

保存顺序固定为：不可变证据/因子包和质量报告→同ID行情库替换（带当前版本CAS）→重新恢复校验保存后的完整哈希→同ID唯一交接最后替换。交接应追加 `latest_attempt`，只有正式门通过才更新 `latest_successful_dataset`。未通过时也记录最新真实云端日期、数量、阻断原因、实际代码提交和保存状态，不能继续只保留9月11日旧尝试。预测库保持独立。

`recovery-validation.yml`使用最近两个已归档交易日的真实报价，验证完整重放、无损去重、冻结计算、重复恢复幂等和截断拒绝。该测试只创建隔离测试库，结果明确不代表原库已更新、250日全池验证或模型通过。验收凭证保存在 `data/evening:validation/latest-recovery.json`。

如果原库文件下载仍失败，属于文件恢复层阻断；云端报价采集仍可继续。不要反复重抓已保存行情，不要把旧交接日期当当前日期。应以失败事实更新原交接，不宣称下次必然可以正式预测。

## 2026-09-20运输与延迟修复

每日任务在保留 Actions artifact 后，将同一真实证据重放校验并发布到独立的 `data/evening` 数据分支。`catalog.json` 列出所有不可变包，`latest.json` 仅指向报价日期最新的运输包；这不是正式研究成功指针。包以512KiB二进制块的base64文本保存，逐块SHA256、拼接后的原ZIP SHA256与原始run_id均记录；通过GitHub文件读取接口可恢复，减少临时下载链接故障造成的中断。不得把校验摘要代替真实重放或把运输成功写成DATA READY。

盘后先读取 `data/evening:catalog.json`，将尚未在原行情库source_log中出现的包按原日期逐一恢复、校验并导入，然后计算因子，保存原身份数据库和唯一交接文件。不要只寻找 `requests/after-close.json`；文件不存在应明确是入口文件缺失，不应推断整个仓库没有权限。`requests/recover-evening.json`只恢复已存在产物，不抓取行情。

GitHub的18:00计划运行若实际延迟到次日00:00—08:59，允许采集紧邻前一个自然日（必须为已验证交易日）的收盘报价。实际抓取时间不变，每条来源时间戳仍必须属于目标日；09:00起禁止这条延迟路线，不将当前行情伪装成历史。周末不会被作为交易日。

任务开关启用、运输可读和描述性因子可恢复均不表示模型通过。正式状态、限价、复权、板块和模型缺口继续阻止正式概率；不得承诺80%盈利胜率。

时区固定为Asia/Shanghai。Web计划任务读取持久文件与GitHub，不依赖个人电脑开机或上次会话的临时目录。两项任务的身份由任务系统保存，不能每次新建重复任务。

## 18:00盘后

1. 检查`trade_calendar.py`的三所公告日历及临时休市公告。周末/休市跳过，未知年份先更新公告证据，不套用政府调休。
2. 恢复唯一当前行情数据库、交接JSON及记录工具。读取仓库main的当前代码并保存提交SHA。包内旧数据库不得覆盖当前库。
3. `python two_stage.py evening-request --db 当前行情库 --output after-close.json`生成带当日日期、真实请求时间和当前数据库哈希的请求。它只请求尚未成功采集的缺失/失败历史，按交易所轮转，不选投资候选。已抓取但实际不足250根的162只短历史单独记录，后续只增量更新，不每天重抓完整历史。最多1000只；云端每批按30分钟与65MB证据预算提前保存断点。未完成者下一批继续，不能标记完整回填。
4. 使用GitHub Contents API把请求原位写入`requests/after-close.json`（已存在时必须读取当前blob SHA再更新）。该路径的push触发`daily-data.yml`。18:00 GitHub cron独立保障当天基础报价采集，但cron不隐式回填历史。若已有相同日期、相同请求的运行，只等待/恢复，不重复提交。
5. 从GitHub读取本次实际run及artifact，保存原包，核验SHA和`source_acceptance.json`的日期、来源、时间。运行`import_probe_artifact.py`必须使用匹配的采集器版本、当前行情库哈希及原库，先`--verify-only`再导入。导入器重放真实响应并保留修订；不会把供应商名单或限价认证成官方状态。归档前完成checkpoint、DELETE日志模式及完整性检查。
6. 对新增行情和已有历史计算基础特征，补充公告、状态、板块与风险，按研究规则逐项检查。尚未实现/未取得的模块明确缺失。完整研究准入和模型准入分别判断，`REAL_DATA_PARTIAL`或GitHub绿色不等于`DATA READY`。
7. 先持久保存原始包、质量报告、池/特征/来源及同ID行情库的新版本，再更新唯一交接指针。失败只更新latest_attempt，保留latest_successful_dataset；不能把旧日期当本次成功。待补历史从原库实际行数恢复，普通日更新不重抓已有250根历史。
8. 记录真实事前预测的实际结果；没有预测就没有命中率。只给用户简洁的日期、数据状态、实际数量/缺口和报告。

运行期间若GitHub有排队或暂时失败，保留断点并有限重试。数据不完整或模型未训练不等于采集入口完全阻断，不因此自动暂停仍能积累真实数据的任务。授权拒绝不绕过。

## 下一交易日09:00

1. 只读恢复前夜行情库和唯一交接文件，不调用daily/backfill，不写行情库。
2. `python two_stage.py morning-check --db 当前行情库 --handoff 当前交接JSON --output morning-check.json`核对上一实际交易日、当前哈希、成功快照及持久引用。周一使用周五，假期顺延。命令只做预检，成功标志也不能替代独立的模型/样本/校准/时间外检查。
3. 合并已保存盘后信息，并搜集前夜至本日09:00实际公开的消息。09:00之后的消息、集合竞价及盘中结果不得混入本版。
4. 按市场→板块→完整5～10元池筛选，且只有数据和模型均合格才输出正式概率和条件入场方案。未合格给实际缺口、证据充分的非全市场观察，无伪TOP和概率。80%是待验证目标，不能承诺。
5. 记录实际生成/保存时间，开盘后才完成排除正式前瞻。只通过记录工具追加同ID预测库；不得回填事前名单。

## 已实现与尚未完成

当前定时运输层具备真实多源名单、当日带时间戳报价、有限历史回填、断点证据、校验导入、休市及晨间只读检查。完整官方证券状态、逐日官方限价、全池历史/板块/时点复权、新闻风险覆盖和校准模型仍须实际完成；本仓库不把这些缺口改为假通过。

GitHub与计划任务可能排队；18:00和09:00是计划启动时间，不承诺秒级启动或立即完成。计划任务与云端cron应每次记录run_id和实际时间。GitHub公共仓库定时工作流长期无活动可能停用，18:00任务的请求提交同时提供可检查的实际活动与补触发。

代码测试只校验边界；真实数据验收必须引用GitHub实际返回的证券数量、日线及数据快照。休市日运行通过只证明正确跳过，不能算完成新行情采集或有效前瞻交易日。

## 2026-09-13真实历史入库版本

供应商范围5,562只证券的3,070,573条历史已按原响应重放核验，并在原行情数据库上追加；原记录保留后共有3,070,574条。5,400只有至少250根OHLCV，162只实际短历史未补造。本次原行情库同身份持久保存已成功，数据库SHA256为`3fbca100c033c344dd3afc9232fe3cf05b008cd4f35509b82786fdbc24e06468`。后续以唯一交接文件的最新版本为准。

`import_shared_history.py`保留原市场数据库身份及既有观察记录。`daily_quotes`为保持原22字段名称的可读写视图，历史数值在`history_quote_facts`，每个原始响应的来源、时间、原始及标准化SHA在`history_source_evidence`。历史观察按evidence_id和trade_date唯一定位，不为每行重复保存同一响应信息；既有与后续修订保留在`quote_observations`和`quote_overrides`。原始归档保持不变，引用bootstrap-evidence-34677204472-1。三市场各570根真实数据已验证数值、采集元数据、重复导入和后续collector写入兼容。不得把`quote_observations`单表行数当成所有历史事实数量；查询日线使用`daily_quotes`。

按正常晚间增量路径导入新报价，不再执行这次完整历史导入。该布局是无损来源去重，不改变数据准入；供应商主表、未知官方证券状态和未知官方限价仍不能当作已验证。运行34741917528另取得3,033,524条带成交额历史，5,334只有至少250条，完整原始归档见amount-evidence-34741917528-1；目前这批成交额仍待独立重放与逐字段并入，不能混同于本次已持久保存的日线。

探索性时间外验证已计算，但正式概率/可成交T+1策略/80%胜率仍未获证实。数据与模型状态仍为DATA NOT READY和model_ready=false。

## 2026-09-21 SQLite跨会话封存修复

从最新同ID文件恢复数据库到每次运行独立的新目录，核对当前版本和SHA256。使用`sealed_database.py restore --source <当前封存库> --expected-sha256 <当前哈希>`创建独立工作副本；拒绝非DELETE模式、非空WAL/回滚日志、应用ID不符或哈希不符的文件，禁止删除活动写入者的日志来绕过验证。恢复工具保留文件身份元数据。

所有增量按旧日期重放并写入隔离副本。导入结束、关闭全部连接并checkpoint后，再用`sealed_database.py inspect`重新打开主文件验证。需要回到工作目录时用`sealed_database.py export --source <封存副本> --destination <全新目标路径>`；不复用有旧SQLite日志的目录。以Library当前版本作并发检查，替换同一行情库ID；保存成功后再更新唯一交接JSON。不能把本地完成、运输成功或描述性因子升格为DATA READY。

若下载返回临时错误，但已有字节与本次读取的当前版本SHA256完全一致的封存副本，可复用该同版本副本；不能使用仅名字相同的旧文件。复制后仍须独立完整性与哈希校验。数据库传送失败时，原库版本和旧成功指针保持不变。
