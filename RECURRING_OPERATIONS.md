# 18:00准备与09:00研究

时区固定为Asia/Shanghai。Web计划任务读取持久文件与GitHub，不依赖个人电脑开机或上次会话的临时目录。两项任务的身份由任务系统保存，不能每次新建重复任务。

## 18:00盘后

1. 检查`trade_calendar.py`的三所公告日历及临时休市公告。周末/休市跳过，未知年份先更新公告证据，不套用政府调休。
2. 恢复唯一当前行情数据库、交接JSON及记录工具。读取仓库main的当前代码并保存提交SHA。包内旧数据库不得覆盖当前库。
3. `python two_stage.py evening-request --db 当前行情库 --output after-close.json`生成带当日日期、真实请求时间和当前数据库哈希的请求。它只请求数据库中不足250根OHLCV的证券，按交易所轮转，不选投资候选。最多1000只；云端每批按30分钟与65MB证据预算提前保存断点。未完成者下一批继续，不能标记完整回填。
4. 使用GitHub Contents API把请求原位写入`requests/after-close.json`（已存在时必须读取当前blob SHA再更新）。该路径的push触发`daily-data.yml`。18:00 GitHub cron独立保障当天基础报价采集，但cron不隐式回填历史。若已有相同日期、相同请求的运行，只等待/恢复，不重复提交。
5. 从GitHub读取本次实际run及artifact，保存原包，核验SHA和`source_acceptance.json`的日期、来源、时间。运行`import_probe_artifact.py`必须使用匹配的采集器版本、当前行情库哈希及原库，先`--verify-only`再导入。导入器重放真实响应并保留修订；不会把供应商名单或限价认证成官方状态。归档前完成checkpoint、DELETE日志模式及完整性检查。
6. 对新增行情和已有历史计算基础特征，补充公告、状态、板块与风险，按研究规则逐项检查。尚未实现/未取得的模块明确缺失。完整研究准入和模型准入分别判断，`REAL_DATA_PARTIAL`或GitHub绿色不等于`DATA READY`。
7. 先持久保存原始包、质量报告、池/特征/来源及同ID行情库的新版本，再更新唯一交接指针。失败只更新latest_attempt，保留latest_successful_dataset；不能把旧日期当本次成功。待补历史从原库实际行数恢复，普通日更新不重抓已有250根历史。
8. 记录真实事前预测的实际结果；没有预测就没有命中率。只给用户简洁的日期、数据状态、实际数量/缺口和报告。

运行期间若GitHub有排队或暂时失败，保留断点并有限重试。数据不完整或模型未训练不等于采集入口完全阻断，不因此自动暂停仍能积累真实数据的任务。授权拒绝不绕过。

## 下一交易日09:00

1. 只读恢复前夜行情库和唯一交接文件，不调用daily/backfill，不写行情库。
2. `python two_stage.py morning-check --db 当前行情库 --handoff 当前交接JSON --output morning-check.json`核对上一实际交易日、当前哈希、成功快照及持久引用。周一使用周五，假期顺延。命令只做预检，成功标志也不能替代独立的模型/样本/校准/时间外检查。
3. 合并已保存盘后信息，并搜集前夜至本日09:00实际公开的消息。09:00之后的消息、集合竞价及盘中结果不得混入本版。
4. 按市场→板块→完整5～10元池筛选，且只有数据和模型均合格才输出正式概率和条件入场方案。未合格给实际缺口、证据充分的非全市场观察，无伪TOP和概率。80%是待验证目标，不能承诺。
5. 记录实际生成/保存时间，开盘后才完成排除正式前瞻。只通过记录工具追加同ID预测库；不得回填事前名单。

## 已实现与尚未完成

当前定时运输层具备真实多源名单、当日带时间戳报价、有限历史回填、断点证据、校验导入、休市及晨间只读检查。完整官方证券状态、逐日官方限价、全池历史/板块/时点复权、新闻风险覆盖和校准模型仍须实际完成；本仓库不把这些缺口改为假通过。

GitHub与计划任务可能排队；18:00和09:00是计划启动时间，不承诺秒级启动或立即完成。计划任务与云端cron应每次记录run_id和实际时间。GitHub公共仓库定时工作流长期无活动可能停用，18:00任务的请求提交同时提供可检查的实际活动与补触发。

代码测试只校验边界；真实数据验收必须引用GitHub实际返回的证券数量、日线及数据快照。休市日运行通过只证明正确跳过，不能算完成新行情采集或有效前瞻交易日。
